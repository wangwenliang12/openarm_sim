# openarm_sim 代码讲解文档（详细函数版）

> 这版文档按你的要求：不讲抽象设计理念，专门讲关键代码本身。重点是：每个函数做什么、参数/返回值类型为什么这样定义、函数内部关键逻辑怎么执行。

---

## 0. 阅读范围（本文覆盖）

本文按以下文件逐个讲：

1. `include/runtime/closed_loop_runtime.hpp`
2. `src/runtime/closed_loop_runtime.cpp`
3. `include/task/manipulate_task.hpp`
4. `src/task/manipulate_task.cpp`
5. `include/state_machine/grasp_state_machine.hpp`
6. `src/state_machine/grasp_state_machine.cpp`
7. `include/task/interpolation_motion_planner.hpp`
8. `src/task/interpolation_motion_planner.cpp`
9. `include/robot/robot_kinematics.hpp`
10. `src/robot/robot_kinematics.cpp`

---

## 1. `ClosedLoopRuntime`（主循环编排）

### 1.1 头文件里的接口在表达什么

`class ClosedLoopRuntime` 是控制主循环入口，三个对外函数：

- `explicit ClosedLoopRuntime(const mjModel* model);`
- `void initialize(const mjModel* model, const mjData* data);`
- `void step(const mjModel* model, mjData* data);`

#### `const mjModel*` 为什么是指针 + const
- `mjModel` 由 MuJoCo 创建并管理生命周期，不在这里 new/delete，所以用指针引用外部对象。
- 这里只读模型结构（joint/actuator/timestep），不修改，所以是 `const`。

#### `initialize(..., const mjData* data)` 为什么 `data` 也是 const
- 初始化阶段只读取 `qpos/qvel/time`，不写 `ctrl/qfrc_applied`。
- 用 `const` 让函数签名直接体现“无副作用写入”。

#### `step(..., mjData* data)` 为什么非 const
- 控制循环必须写：
  - `data->ctrl[]`（执行器命令）
  - `data->qfrc_applied[]`（重力补偿）
- 所以必须可写。

### 1.2 私有结构 `MjJointMap`（最关键的数据结构之一）

字段：
- `std::string name`
- `int mj_joint_id`
- `int mj_qpos_adr`
- `int mj_dof_adr`
- `int pin_q_adr`
- `int pin_v_adr`
- `int actuator_id`

它解决的核心问题：**同名关节在 MuJoCo 与 Pinocchio 的索引顺序不同**。

- `mj_qpos_adr / mj_dof_adr`：告诉你这个关节在 MuJoCo 状态向量里的位置。
- `pin_q_adr / pin_v_adr`：告诉你这个关节在 Pinocchio 状态向量里的位置。
- `actuator_id`：告诉你命令写到哪个 `ctrl` 槽。

这样每个控制周期只需按表搬运，不需要反复字符串查找。

---

### 1.3 `src/runtime/closed_loop_runtime.cpp` 逐函数讲解

#### A) 匿名命名空间常量

- `kChannel`：LCM 通道名。
- `kTargetObjectId`：状态机关注目标。
- `kStateMachineConfigPath`：JSON 配置路径。
- 速度/容差常量：控制平滑、轨迹判断会用。
- `kLeftArmJoints/kRightArmJoints`：构造 `ManipulateTask` 时给运动学层“关节子集定义”。

这些做成 `constexpr`/`const` 使其只在本翻译单元可见，避免外部符号污染。

#### B) `const char* stateName(GraspStateMachine::State state)`

用途：日志输出，把枚举转换成人可读字符串。

返回 `const char*` 而不是 `std::string`：
- 字面量常量无需动态分配。
- 日志函数频繁调用，返回指针更轻量。

---

#### C) `ClosedLoopRuntime::ClosedLoopRuntime(const mjModel* model)`

做的事情按顺序：
1. 构造 `task_`（给 URDF、末端 frame、左右臂关节列表）。
2. 构造 LCM 发布/订阅器。若任一未就绪，抛异常。
3. `setupJointMaps(model)` 建立 MuJoCo-Pinocchio-Actuator 的关节映射。
4. `publisher_.initialize(model)` 初始化对象真值发布器。
5. 读取 `model->opt.timestep` 存到 `control_timestep_`。
6. 构造并设置 `home_q`（动态维 `VectorXd`，长度为 `kinematics().nq()`）。

为什么 `home_q` 用 `VectorXd::Zero(nq)`：
- 自由度来自模型，不固定。
- 未手动赋值的关节默认保留 0，写法清晰。

---

#### D) `void initialize(const mjModel* model, const mjData* data)`

关键步骤：
1. `syncRobotStateToTask(data)`：把 MuJoCo 当前状态同步给运动学层。
2. `task_.getEndEffectorPose()` 读当前末端位姿。
3. `state_machine_.initialize(data->time, ee_pose.translation(), config_path)`。
4. `task_.loadNamedPoses(state_machine_.namedJointPoses())`。
5. `task_.setJointAngles(task_.kinematics().currentQ())` 防止第一帧平滑器跳变。
6. `last_state_ = state_machine_.state()`。

参数里 `model` 当前未使用，所以 `(void)model;` 用于消除未使用告警。

---

#### E) `void step(const mjModel* model, mjData* data)`

这是主循环，执行顺序非常固定：

1. `publisher_.publishTruthPoses(model, data)` 发布仿真真值。
2. `subscriber_.poll()` 拉取最新感知消息。
3. `syncRobotStateToTask(data)` 同步关节状态。
4. 计算 `ee_pose/ee_pos`。
5. `idle_pose_reached = task_.isNamedPoseReached("idle", tol)`。
6. 尝试获取目标物体观测：
   - 成功 -> `observed_pose_ptr = &observed_pose`
   - 失败 -> `observed_pose_ptr = nullptr`
7. `command = state_machine_.update(...)` 生成命令。
8. 检测状态切换：若切换，重新 `update(...)` 获取新状态命令，并通知 `task_.onStateTransition(...)`。
9. `task_.ensureTrajectoryForState(command, state_id, data->time)`。
10. `q_cmd = task_.sampleJointTargets(data->time)`。
11. `maybeLogGraspDebug(...)`（close/lift 状态节流日志）。
12. `limits = controlLimitsForState()`。
13. `q_cmd = task_.smoothJointTargets(q_cmd, limits, control_timestep_)`。
14. 输出控制：
    - `applyRobotGravityCompensation(data)`
    - `writeArmPositionControls(data, q_cmd)`
    - `writeFingerControls(data, command.left_gripper_cmd, command.right_gripper_cmd)`
    - `writeHeadControls(data)`
15. `last_state_ = current_state`。

这里每一步都保持“输入->处理->输出”顺序，便于调试定位。

---

#### F) `setupJointMaps(const mjModel* model)`

逻辑：
1. 清空 `mj_joints_`。
2. 遍历 `task_.kinematics().joints()`（Pinocchio 关节表）。
3. 对每个关节：
   - 用关节名 `mj_name2id(..., mjOBJ_JOINT, name)` 找 MuJoCo joint id。
   - 读 `model->jnt_qposadr[id]`、`model->jnt_dofadr[id]`。
   - 写入 Pinocchio 地址 `pin_q_adr/pin_v_adr`。
4. 最后调用 `setupActuatorIds(model)` 补全执行器映射。

异常策略：关节找不到直接 `throw runtime_error`，避免“部分映射成功”导致后续 silent bug。

---

#### G) `setupActuatorIds(const mjModel* model)`

职责：把每个关节映射到 actuator。

实现方式：通过关节命名规则拼 actuator 名称（如 `left_jointX_ctrl` / `right_finger1_ctrl`），再 `mj_name2id(..., mjOBJ_ACTUATOR, ...)`。

为什么需要单独函数：
- 关节映射和执行器映射是两个维度，拆开后更易维护。
- 后续若命名规则变，只改这里。

另外还单独缓存头部执行器 `head_actuator_1_/2_`。

---

#### H) `syncRobotStateToTask(const mjData* data)`

逻辑非常直接：
1. 依据 `nq/nv` 创建 `q`、`dq`。
2. 遍历 `mj_joints_`：
   - `q[pin_q_adr] = data->qpos[mj_qpos_adr]`
   - `dq[pin_v_adr] = data->qvel[mj_dof_adr]`
3. 调 `task_.setRobotState(q, dq)`。

本质是“跨引擎状态搬运函数”。

---

#### I) `ControlLimits controlLimitsForState() const`

输入：无显式参数，读当前状态机状态。
输出：`ControlLimits` 值对象。

规则：
- `Descend/Close/Lift`：使用更快/更适配的关节与手指速度。
- 其他状态：使用常规速度。

返回值对象而非写类成员：
- 避免全局可变共享状态。
- 调用点能清楚看到“本帧限速配置”。

---

#### J) `applyRobotGravityCompensation(mjData* data) const`

把 `qfrc_bias` 拷到 `qfrc_applied`（仅机器人关节）。

意义：在位置控制外，额外补上模型偏置力矩，减轻执行器负担，提高跟踪稳定性。

---

#### K) `writeArmPositionControls(mjData* data, const Eigen::VectorXd& q) const`

- 遍历关节映射。
- 仅处理名字包含 `joint` 的关节。
- `data->ctrl[actuator_id] = q[pin_q_adr]`。

`q` 用 `const Eigen::VectorXd&`：
- 维度动态。
- 避免拷贝。

---

#### L) `writeFingerControls(mjData* data, double left, double right) const`

步骤：
1. `left/right` 先 `clamp` 到 `[0,1]`。
2. 乘 `0.044` 转成实际开度。
3. 按左右手指关节名写入对应 actuator。

为什么参数是 `double`：支持连续开合，不是二值开关。

---

#### M) `writeHeadControls(mjData* data) const`

如果头部 actuator 存在，固定写 0。目的：保持头部关节静止，不参与当前抓取任务。

---

#### N) `maybeLogGraspDebug(...)`

参数：
- `double now`
- `const pinocchio::SE3& ee_pose`
- `const GraspStateMachine::Command& command`
- `const ObjectPose* observed_pose`

逻辑：
- 只在 `Close/Lift` 打印。
- 0.1s 节流一次。
- 输出末端位置、夹爪命令、物块位置差值，Lift 状态额外打目标点。

`observed_pose` 用指针是为了表达“可能缺观测”。

---

## 2. `ManipulateTask`（从状态机命令到关节目标）

### 2.1 头文件关键类型

#### `struct ControlLimits`
- `double arm_joint_velocity`
- `double finger_velocity`

只放数值，不放逻辑，方便 runtime 构造后传入。

#### `using NamedPoseMap = unordered_map<string, unordered_map<string, double>>`

语义：`pose_name -> (joint_name -> target_value)`。

两层 map 的好处：
- 配置到内存结构映射直接。
- 按姿态名和关节名查找都是平均 O(1)。

---

### 2.2 `src/task/manipulate_task.cpp` 逐函数讲解

#### A) 构造函数

初始化顺序：
1. `kinematics_`（URDF + frame + 关节子集）
2. `interpolation_planner_`
3. `ompl_planner_`（若无 OMPL 则回退插值规划器）
4. `smoothed_q_cmd_ = Zero(nq)`

这里用 `std::unique_ptr<IMotionPlanner>`：
- 统一多种规划器接口（多态）。
- 独占所有权，生命周期清晰。

---

#### B) `setRobotState(const VectorXd& q, const VectorXd& dq)`

薄封装：直接把状态传给 `kinematics_`。存在意义是隔离调用层，不让 runtime 直接操作运动学细节。

---

#### C) `pinocchio::SE3 getEndEffectorPose()`

返回末端位姿（旋转+平移）。用 `SE3` 比拆成 `Vector3 + Quaternion` 更适合与 Pinocchio API 对接。

---

#### D) `loadNamedPoses(const NamedPoseMap& poses)`

详细流程：
1. 清空旧缓存 `named_pose_targets_`。
2. 拿到 joint 列表与模型限位。
3. 遍历每个姿态：
   - 从 `currentQ()` 拷一份 `q_pose`。
   - 对姿态里每个 joint_name：
     - 在 joint 列表里匹配。
     - 用模型上下限 `clamp` 后写入 `q_pose`。
     - 若找不到关节名，抛异常。
4. 缓存 `named_pose_targets_[pose_name] = q_pose`。

“以 currentQ 为底板再覆盖”可以让未配置关节继承当前值，避免未定义关节突变。

---

#### E) `getNamedPoseTargets(const string& name) const`

- 从 map 查询。
- 找不到直接抛异常。
- 返回 `VectorXd` 值对象（调用方拿副本，避免外部误改内部缓存）。

---

#### F) `isNamedPoseReached(const string& name, double tol) const`

判据：`(currentQ - q_target).abs().maxCoeff() < tol`。

为什么用 `maxCoeff`：要求“所有关节都进入容差”才算达到，条件更严格。

---

#### G) `onStateTransition(int prev_state_id, int next_state_id, double now)`

逻辑：
1. 如果当前轨迹存在且 owner 不是新状态，清轨迹。
2. 若新状态是 `Done`，调用 `captureHoldPosition()` 并返回。
3. 否则关闭 hold 模式。

参数用 `int state_id` 而不是直接 enum：便于和通用轨迹 owner id 对接，减少模板/枚举耦合。

---

#### H) `ensureTrajectoryForState(const Command& command, int state_id, double now)`

作用：保证当前状态有可执行轨迹。

步骤：
1. `stateRequiresArmPlan(command)` 为 false -> 直接返回 false（本状态不需要规划）。
2. 如果已有 active trajectory 且 owner_state_id 一致 -> 复用，返回 true。
3. 构建 `MotionRequest request = buildMotionRequest(...)`。
4. 根据 request 选 planner。
5. `trajectory = planner.createPlan(request)`。
6. `executor_.setTrajectory(trajectory, now)`。
7. 输出日志并返回 `trajectory.valid`。

返回 bool 的意义：上层可直接知道本帧是否有有效轨迹。

---

#### I) `sampleJointTargets(double now) const`

分支：
- 无活动轨迹：
  - 若 hold 激活且维度匹配，返回 `hold_q_`
  - 否则返回 `currentQ()`
- 有轨迹：`executor_.sample(now)`

保证任何时刻都有“可下发目标关节向量”。

---

#### J) `buildMotionRequest(const Command& command, int state_id) const`

内容填充：
- `owner_state_id`
- `start_q = currentQ()`
- 某些状态提高 `max_joint_velocity`
- 若命令是 `JointPose` -> `goal_type=JointGoal` + `joint_goal_q`
- 若是 `CartesianPose` -> `goal_type=CartesianPoseGoal` + `target_pos/target_quat`

这是把“状态机语义命令”翻译为“规划器输入”的关键函数。

---

#### K) `stateRequiresArmPlan(const Command& command) const`

直接返回 `command.requires_arm_plan`。

存在价值：统一封装判据。后续若加复杂条件，不改上层调用。

---

#### L) `plannerForRequest(const MotionRequest& request) const`

- `JointGoal` -> 插值规划器
- `CartesianPoseGoal` -> OMPL（或回退 planner）

把“目标类型到规划器”的路由集中在一个函数。

---

#### M) `captureHoldPosition()`

`hold_q_ = currentQ(); hold_position_active_ = true;`

用于 Done 状态锁住当前姿态，避免轨迹结束后回弹。

---

#### N) `smoothJointTargets(const VectorXd& q_target, const ControlLimits& limits, double dt) const`

核心算法（逐关节限速）：
1. 若 `smoothed_q_cmd_` 维度不匹配，重置为 `currentQ()`。
2. 遍历每个关节索引 i：
   - 默认步长上限 `arm_joint_velocity * dt`
   - 若是 finger 关节，改用 `finger_velocity * dt`
   - `delta = q_target[i] - smoothed_q_cmd_[i]`
   - `smoothed_q_cmd_[i] += clamp(delta, -step_limit, step_limit)`
3. 返回 `smoothed_q_cmd_`

为什么函数是 `const` 但能改 `smoothed_q_cmd_`：
- 成员被声明为 `mutable`，表示这是“缓存/平滑内部状态”，不改变任务语义。

---

#### O) `setJointAngles(const VectorXd& q_target)`

直接把平滑内部状态对齐到某个目标。常用于初始化阶段消除突跳。

---

#### P) `kinematics()` / `kinematics() const`

返回引用而非拷贝：
- `RobotKinematics` 体量大且包含模型数据，不应复制。
- 非 const/const 双重接口满足读写场景。

---

## 3. `GraspStateMachine`（离散状态到连续命令）

### 3.1 头文件关键结构

#### `enum class CommandMode`
- `JointPose`
- `CartesianPose`

#### `enum class State`
- `Home, Pregrasp, Descend, Close, Lift, Done`

#### `struct Command`
- `mode`
- `joint_pose_name`
- `target_pos`
- `target_quat`
- `left_gripper_cmd/right_gripper_cmd`
- `requires_arm_plan`

这使状态机输出统一格式；任务层按 `mode` 决定解释方式。

---

### 3.2 `src/state_machine/grasp_state_machine.cpp` 逐函数讲解

#### A) `loadJointGroup(const json& group, unordered_map<string,double>* joint_positions)`

- 校验 `group` 必须是 object。
- 遍历每个 key/value。
- 校验 value 为数字。
- 写入 `joint_positions`。

第二参数用指针：
- 允许原地填充。
- 明确可能为空（尽管当前调用场景通常非空）。

---

#### B) `makeTopDownGraspQuat(const Quaterniond& object_quat)`

步骤：
1. 物体四元数归一化。
2. 取物体 x 轴方向向量。
3. 用 `atan2(y,x)` 求 yaw。
4. 组合 `yaw_rotation * tool_alignment(pi around Y)`。
5. 结果再 normalize。

返回 `Quaterniond`：更适配 IK 输入，避免欧拉角奇异。

---

#### C) 构造 `GraspStateMachine(const string& target_object_id)`

只做成员初始化：锁定目标 object id。

---

#### D) `initialize(double now, const Vector3d& current_pos, const string& config_path)`

执行：
1. `loadConfig(config_path)`。
2. 状态置 `Home`，记录 `state_start_time_`。
3. `object_pose_` 和 `grasp_target_pose_` 初始化为当前位置。
4. 重置 pregrasp/lift 起点与布尔标志。

为什么传 `current_pos`：即使暂时无观测，也有安全默认目标。

---

#### E) `Command update(double now, const Vector3d& current_pos, const ObjectPose* observed_pose, bool active_joint_pose_reached)`

流程：
1. 若观测存在，更新 `object_pose_`。
2. 若尚未锁定抓取目标，同步 `grasp_target_pose_`。
3. 根据 `state_` 分派到 `handleHome/Pregrasp/...`。
4. 返回该状态命令。

`observed_pose` 可空指针是为了表达“本帧可能没有感知”。

---

#### F) 只读访问器

- `State state() const`
- `const string& targetObjectId() const`
- `const NamedPoseMap& namedJointPoses() const`

都返回轻量值/const 引用，避免拷贝。

---

#### G) `loadConfig(const string& config_path)`

主要做 JSON 读取与字段解析：
- 基础时序/高度/容差参数
- `named_joint_poses`

异常策略：文件打不开、字段非法直接抛异常，避免运行在不完整配置上。

---

#### H) `transitionTo(State next, double now)`

- 打印状态迁移日志。
- `state_ = next`
- `state_start_time_ = now`
- 某些状态切换时调整内部标志（例如锁定目标姿态）

它把“状态迁移时的副作用”集中管理。

---

#### I) `stateName(State state)`

枚举转字符串，供日志打印。

---

#### J) `handleHome(...)`

输出命令：
- `mode=JointPose`
- `joint_pose_name="idle"`
- 左右夹爪打开
- `requires_arm_plan=true`

若 `shouldLeaveHome(...)` 为真，转到 `Pregrasp`。

---

#### K) `shouldLeaveHome(bool active_joint_pose_reached, double now) const`

条件：
- 若 `home_only_mode` 开启 -> 永不离开 Home。
- 否则要求：已到 idle + 在 Home 停留超过 `home_duration_sec`。

---

#### L) `handlePregrasp(...)`

- 首次进入时记录 `pregrasp_start_pos_`。
- 生成笛卡尔命令：
  - 姿态：`makeTopDownGraspQuat`
  - 位置：`grasp_target_pose_.position + [0,0,pregrasp_height]`
  - 夹爪开，`requires_arm_plan=true`
- 若当前位置到目标距离 < `pregrasp_tolerance`，转 `Descend`。

---

#### M) `handleDescend(...)`

与 Pregrasp 类似，但目标高度换成 `descend_height`。到达容差后进入 `Close`。

---

#### N) `handleClose(...)`

- 保持末端在抓取点上方小高度。
- 左夹爪开，右夹爪闭（按当前任务设定）。
- `requires_arm_plan=false`（不重新规划臂轨迹，只执行夹爪动作）。
- `close_duration_sec` 到时记录 `lift_start_pos_` 并转 `Lift`。

---

#### O) `handleLift(...)`

- 目标点：`lift_start_pos_ + [0,0,lift_height]`
- 姿态保持顶抓
- 右夹爪维持闭合
- 需规划臂运动
- 到达容差后转 `Done`

---

#### P) `handleDone(...)`

输出“保持 Lift 终点附近”的命令：
- 夹爪保持抓取配置
- `requires_arm_plan=false`

用于任务结束稳定保持。

---

## 4. `InterpolationMotionPlanner`（关节插值规划）

### 4.1 头文件

- 构造：`explicit InterpolationMotionPlanner(RobotKinematics& kinematics)`
- 主函数：`PlannedTrajectory createPlan(const MotionRequest& request) override`
- 私有：`Eigen::VectorXd resolveGoalQ(const MotionRequest& request) const`

`RobotKinematics&` 用引用：规划器不拥有运动学对象，只借用。

---

### 4.2 `src/task/interpolation_motion_planner.cpp` 逐函数

#### A) 构造函数

保存 `kinematics_` 引用。

#### B) `createPlan(const MotionRequest& request)`

步骤：
1. 初始化空轨迹并写 `owner_state_id`。
2. 若 `start_q` 为空，返回无效轨迹。
3. `goal_q = resolveGoalQ(request)`。
4. 若维度不匹配，返回无效轨迹。
5. 计算最大关节变化量 `max_delta`。
6. 计算轨迹时长 `duration = max(0.25, max_delta / max_joint_velocity)`。
7. 采样 `kNumSegments+1` 个点。
8. 每个点用 smoothstep 插值：`a^2(3-2a)`。
9. 填充 `time_from_start` 与 `q`。
10. 标记 `trajectory.valid=true` 并返回。

---

#### C) `resolveGoalQ(const MotionRequest& request) const`

- 若目标是 `JointGoal`：直接返回 `request.joint_goal_q`。
- 否则：调用 `kinematics_.solveRightArmIK(target_pos, target_quat, start_q)`。

这是把不同目标表示统一成关节目标向量。

---

## 5. `RobotKinematics`（Pinocchio模型、FK、IK）

### 5.1 头文件接口意义

核心接口：
- `setRobotState(q, dq)`
- `currentQ()`
- `getEndEffectorPose()`
- `solveRightArmIK(...)`
- `solveIK(...)`
- 若干右臂子空间工具函数（extract/merge/bounds/dof）

全部围绕“动态维状态向量 + 指定末端 frame + 右臂求解”展开。

---

### 5.2 `src/robot/robot_kinematics.cpp` 逐函数讲解

#### A) 构造函数 `RobotKinematics(...)`

主要工作：
1. 从 URDF 构建 `pin_model_`。
2. 创建 `pin_data_`。
3. 按关节名筛选可控关节，填充 `joints_`。
4. 建立左右臂索引数组 `right_arm_indices_/left_arm_indices_`。
5. 查末端 frame id `ee_frame_id_`。
6. 初始化 `current_q_ / current_dq_ / home_q_`。

其中 `JointInfo` 缓存每个关节的：名称、q/v 地址、限位、是否 finger 等元信息。

---

#### B) `setRobotState(const VectorXd& q, const VectorXd& dq)`

保存当前状态并用于后续 FK/IK。

#### C) `const VectorXd& currentQ() const`

返回当前关节角 const 引用，避免拷贝。

#### D) `SE3 getEndEffectorPose()`

调用 Pinocchio：
- `forwardKinematics`
- `updateFramePlacements`
- 返回 `pin_data_.oMf[ee_frame_id_]`

---

#### E) `setHomeQ` / `homeQ`

设置与读取 home 姿态。

---

#### F) `joints()` / `nq()` / `nv()` / `model()`

都是基础访问器，供上层构造映射、限位、维度判断。

---

#### G) `extractRightArmQ(const VectorXd& full_q) const`

按 `right_arm_indices_` 从全身向量抽取右臂子向量。

用途：供右臂子空间优化/边界约束使用。

---

#### H) `mergeRightArmQ(const VectorXd& right_arm_q, const VectorXd& base_q) const`

把右臂子向量写回全身向量，其余关节保持 `base_q`。

---

#### I) `rightArmLowerBounds()` / `rightArmUpperBounds()` / `rightArmDof()`

分别返回：右臂下界、上界、自由度数量。

---

#### J) `solveRightArmIK(...)`

薄封装，内部直接调用 `solveIK(...)`。

存在意义：语义清晰地表明“这是右臂 IK 接口”。

---

#### K) `solveIK(const Vector3d& target_pos, const Quaterniond& target_quat, const VectorXd& seed_q) const`

这是最关键函数，按迭代过程分解：

1. `q_cmd = seed_q`。
2. 把左臂关节强制置为 `home_q_`（锁定左臂，避免双臂耦合扰动）。
3. 目标四元数归一化。
4. 进入最多 80 次迭代：
   - 计算当前 FK、雅可比、frame 位姿。
   - 位置误差：`target_pos - current_pos`。
   - 旋转误差：`quat_error = target * current.conjugate()`。
   - 若 `quat_error.w() < 0`，整体取反，保证最短弧。
   - 角轴表示得到旋转误差向量。
   - 组装 6D 任务误差 `task_error`（位置3 + 姿态3，姿态带权重 0.35）。
   - 收敛判断：位置和姿态误差都小于阈值则 break。
   - 计算全雅可比后抽取右臂列 `jacobian_right`。
   - 同时抽取 `q_seed_right/q_current_right`。
   - 阻尼伪逆：
     - `lhs = J J^T + λI`
     - `J^+ = J^T lhs^{-1}`
   - 零空间项：
     - `N = I - J^+ J`
     - `seed_bias = 0.15*(q_seed_right - q_current_right)`
   - 合成增量：`delta_q = J^+ * task_error + N * seed_bias`
   - 更新右臂关节：`q_cmd += 0.4 * delta_q`
   - 每轮后按模型上下限 clamp 所有关节。
5. 返回 `q_cmd`。

类型选择原因：
- `target_pos: Vector3d`（维度固定、语义清楚）
- `target_quat: Quaterniond`（姿态无奇异）
- `seed_q: VectorXd`（模型维度动态）
- 返回 `VectorXd`（直接可作为控制目标）。

---

## 6. 一帧控制周期，把函数串起来看

每个 `step()` 周期中，函数调用链可以写成：

1. `publishTruthPoses` / `poll`
2. `syncRobotStateToTask`
3. `getEndEffectorPose`
4. `isNamedPoseReached`
5. `state_machine.update`
6. `onStateTransition`（如有切换）
7. `ensureTrajectoryForState`
8. `sampleJointTargets`
9. `smoothJointTargets`
10. `applyRobotGravityCompensation`
11. `writeArmPositionControls`
12. `writeFingerControls`
13. `writeHeadControls`

这 13 步就是“观测->状态->规划->平滑->下发”的实际代码执行路径。

---

## 7. 你想要“接近逐行”的阅读建议

如果你下一步希望我继续细化到“真逐行注释版”，可以按下面顺序继续拆：

1. 先把 `src/runtime/closed_loop_runtime.cpp` 做成逐行注释版（最核心主循环）。
2. 再拆 `src/task/manipulate_task.cpp`（轨迹与平滑）。
3. 再拆 `src/robot/robot_kinematics.cpp` 的 `solveIK`（数学最多）。
4. 最后补状态机文件。

这版已经做到“每个函数都解释了用途、参数、返回值与关键逻辑”，如果你要，我可以在下一版直接给你“函数源码块 + 行内注释”的格式。
