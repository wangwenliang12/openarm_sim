# openarm_sim_cpp 代码讲解

## 1. 项目在做什么

这个项目是一个双臂 OpenArm 的 MuJoCo 仿真控制系统，重点不是单个控制器，而是一条完整闭环链路：

`MuJoCo 仿真 -> LCM 目标位姿 -> 状态机决策 -> 运动学/规划 -> 关节命令 -> 仿真执行`

仓库里有两条入口：

- `python_runner.py` + `openarm_core.so`
  用来验证基础控制器。
- `python_closed_loop_runner.py` + `openarm_closed_loop.so`
  用来跑完整抓取流程，是主阅读路径。

如果只想快速理解代码，优先看这几个文件：

1. `python_closed_loop_runner.py`
2. `src/closed_loop_interface.cpp`
3. `src/runtime/closed_loop_runtime.cpp`
4. `src/state_machine/grasp_state_machine.cpp`
5. `src/task/manipulate_task.cpp`
6. `src/robot/robot_kinematics.cpp`

## 2. 代码主流程

闭环版本每个控制周期都会执行一次 `ClosedLoopRuntime::step()`。它基本就是整个系统的主循环：

```cpp
void ClosedLoopRuntime::step(const mjModel* model, mjData* data) {
    publisher_.publishTruthPoses(model, data);
    subscriber_.poll();

    syncRobotStateToTask(data);

    const pinocchio::SE3 ee_pose = task_.getEndEffectorPose();
    const bool idle_pose_reached =
        task_.isNamedPoseReached("idle", kJointPoseReachedTolerance);

    ObjectPose observed_pose;
    const ObjectPose* observed_pose_ptr =
        subscriber_.getPose(state_machine_.targetObjectId(), &observed_pose)
            ? &observed_pose : nullptr;

    GraspStateMachine::Command command =
        state_machine_.update(data->time, ee_pose.translation(),
                              observed_pose_ptr, idle_pose_reached);

    task_.ensureTrajectoryForState(command, state_id, data->time);
    Eigen::VectorXd q_cmd = task_.sampleJointTargets(data->time);
    q_cmd = task_.smoothJointTargets(q_cmd, limits, control_timestep_);

    applyRobotGravityCompensation(data);
    writeArmPositionControls(data, q_cmd);
    writeFingerControls(data, command.left_gripper_cmd, command.right_gripper_cmd);
}
```

这段代码可以直接按 4 步理解：

1. 先拿数据
   MuJoCo 当前状态、LCM 目标物体位姿、末端当前位姿。
2. 再做决策
   状态机决定当前应该回 home、预抓取、下探、闭合还是抬升。
3. 再生成关节目标
   `ManipulateTask` 把“动作意图”转成轨迹和关节角。
4. 最后写回仿真
   关节目标写到 `data->ctrl`，补偿力写到 `data->qfrc_applied`。

所以 `ClosedLoopRuntime` 不是算法本体，而是总调度器。

## 3. 核心模块怎么分工

### 3.1 `ClosedLoopRuntime`

文件：

- `include/runtime/closed_loop_runtime.hpp`
- `src/runtime/closed_loop_runtime.cpp`

职责只有一个：把所有模块串起来。

它主要做三件事：

- 同步 MuJoCo 与 Pinocchio 的关节状态
- 调用状态机和规划模块
- 把最终命令写回 MuJoCo

这里最重要的设计是“关节映射表”。MuJoCo 和 Pinocchio 都有自己的索引，不能假设下标一致，所以初始化时先建表，运行时直接按表读写。

关键代码：

```cpp
for (const auto& map : mj_joints_) {
    q[map.pin_q_adr] = data->qpos[map.mj_qpos_adr];
    dq[map.pin_v_adr] = data->qvel[map.mj_dof_adr];
}
task_.setRobotState(q, dq);
```

解释：

- `mj_qpos_adr` / `mj_dof_adr` 是 MuJoCo 的索引
- `pin_q_adr` / `pin_v_adr` 是 Pinocchio 的索引
- 这一步把仿真状态拷贝到运动学模块，后面的 FK、IK、轨迹规划才有真实输入

### 3.2 `GraspStateMachine`

文件：

- `include/state_machine/grasp_state_machine.hpp`
- `src/state_machine/grasp_state_machine.cpp`

它只回答一个问题：机器人“下一步要做什么”。

状态顺序很简单：

`Home -> Pregrasp -> Descend -> Close -> Lift -> Done`

每个状态输出一个 `Command`，核心字段包括：

- `mode`
  目标是关节姿态还是笛卡尔位姿
- `joint_pose_name`
  例如 `"idle"`
- `target_pos` / `target_quat`
  笛卡尔抓取目标
- `left_gripper_cmd` / `right_gripper_cmd`
  夹爪开合
- `requires_arm_plan`
  这一帧是否需要重新生成机械臂轨迹

关键代码：

```cpp
GraspStateMachine::Command GraspStateMachine::handlePregrasp(
    double now, const Eigen::Vector3d& current_pos) {
    Command cmd;
    cmd.mode = CommandMode::CartesianPose;
    cmd.target_quat = makeTopDownGraspQuat(grasp_target_pose_.quaternion);
    cmd.target_pos =
        grasp_target_pose_.position + Eigen::Vector3d(0.0, 0.0, config_.pregrasp_height);
    cmd.right_gripper_cmd = kGripperOpen_;
    cmd.requires_arm_plan = true;

    if ((current_pos - cmd.target_pos).norm() < config_.pregrasp_tolerance) {
        transitionTo(State::Descend, now);
    }
    return cmd;
}
```

解释：

- `Pregrasp` 不是直接抓，而是先移动到物体正上方
- 姿态由 `makeTopDownGraspQuat()` 生成，表示末端采用俯抓朝向
- 当末端位置误差小于阈值时，再切到 `Descend`

这个设计的意义是：状态机只负责“阶段切换”，不负责具体求逆解或插值。

### 3.3 `ManipulateTask`

文件：

- `include/task/manipulate_task.hpp`
- `src/task/manipulate_task.cpp`

它负责把状态机命令变成可执行的关节轨迹。

你可以把它看成一层翻译器：

- 输入：`Command`
- 输出：`q_cmd`

内部做了三件事：

1. 把命名姿态加载成完整关节向量
2. 根据命令类型选择规划器
3. 对轨迹输出做限速和平滑

关键代码：

```cpp
if (command.mode == GraspStateMachine::CommandMode::JointPose) {
    request.goal_type = GoalType::JointGoal;
    request.joint_goal_q = getNamedPoseTargets(command.joint_pose_name);
} else {
    request.goal_type = GoalType::CartesianPoseGoal;
    request.target_pos = command.target_pos;
    request.target_quat = command.target_quat;
}
```

解释：

- 如果状态机给的是 `"idle"` 这类命名姿态，就走关节空间目标
- 如果给的是抓取点位姿，就走笛卡尔目标
- 这样状态机不需要关心底层规划细节，只表达意图

它后面还会做一次平滑限速，避免命令跳变：

```cpp
const double delta = q_target[i] - smoothed_q_cmd_[i];
smoothed_q_cmd_[i] += std::clamp(delta, -step_limit, step_limit);
```

这段代码的作用很直接：每一帧最多只允许关节走一小步，防止目标切换时瞬间跳太大。

### 3.4 `RobotKinematics`

文件：

- `include/robot/robot_kinematics.hpp`
- `src/robot/robot_kinematics.cpp`

这个类负责所有几何计算，核心依赖是 Pinocchio。

主要能力：

- 读取 URDF 建模
- 正运动学求末端位姿
- 逆运动学求右臂关节角
- 固定左臂在 home 附近，只让右臂参与抓取

最关键的是 IK 求解。它是一个迭代法，每次根据末端误差更新右臂关节：

```cpp
const Eigen::MatrixXd jacobian_pinv =
    jacobian_right.transpose() * lhs.ldlt().solve(Eigen::MatrixXd::Identity(6, 6));

const Eigen::MatrixXd nullspace =
    Eigen::MatrixXd::Identity(n_right, n_right) - jacobian_pinv * jacobian_right;
const Eigen::VectorXd seed_bias = 0.15 * (q_seed_right - q_current_right);
const Eigen::VectorXd delta_q = jacobian_pinv * task_error + nullspace * seed_bias;
```

解释：

- `jacobian_pinv * task_error`
  负责让末端往目标位姿靠近
- `nullspace * seed_bias`
  负责在不影响末端任务的前提下，让解不要偏离初始姿态太多
- `seed_q` 很重要
  它让 IK 解更稳定，也减少跳到奇怪姿态的概率

这里还有一个很重要的约束：

```cpp
for (const int idx : left_arm_indices_) {
    const auto& ji = joints_[static_cast<size_t>(idx)];
    q_cmd[ji.pin_q_adr] = home_q_[ji.pin_q_adr];
}
```

意思是左臂基本锁在 home 位姿，抓取任务主要由右臂完成。这样会少掉很多双臂协调复杂度。

### 3.5 `InterpolationMotionPlanner`

文件：

- `include/task/interpolation_motion_planner.hpp`
- `src/task/interpolation_motion_planner.cpp`

这是最基础的规划器，逻辑很简单：起点和终点已知，就做平滑插值。

关键代码：

```cpp
const double smooth_alpha = alpha * alpha * (3.0 - 2.0 * alpha);
point.q = request.start_q + smooth_alpha * (goal_q - request.start_q);
```

解释：

- 不是简单线性插值，而是用了一个平滑 S 曲线
- 好处是起点和终点速度更柔和，不容易产生突变

对于关节目标，这个规划器已经够用；对于笛卡尔抓取目标，则会先经过 IK 把目标位姿转成关节角。

## 4. 抓取任务到底怎么跑起来

完整流程可以压缩成下面这张脑图：

1. Python 加载 MuJoCo 模型，并通过 `ctypes` 调用 C++ 动态库
2. `sim_init()` 创建 `ClosedLoopRuntime`
3. 每一帧 `sim_step()` 调用 `ClosedLoopRuntime::step()`
4. `step()` 同步机器人状态，读取目标物体位姿
5. 状态机根据当前阶段输出 `Command`
6. `ManipulateTask` 把 `Command` 变成轨迹与关节目标
7. 控制命令写入 MuJoCo，机器人开始运动

如果只记一句话，可以记成：

`状态机定方向，ManipulateTask 产轨迹，RobotKinematics 负责几何求解，ClosedLoopRuntime 负责串全局。`

## 5. 推荐阅读顺序

如果你准备继续深入代码，建议按下面顺序看：

1. `src/runtime/closed_loop_runtime.cpp`
   先抓住主循环，知道每一帧发生了什么。
2. `src/state_machine/grasp_state_machine.cpp`
   再看任务阶段怎么切换。
3. `src/task/manipulate_task.cpp`
   看状态机命令怎么转成轨迹。
4. `src/robot/robot_kinematics.cpp`
   最后看 FK/IK 的具体实现。

这样读会比从单个算法文件开始更容易建立全局认知。
