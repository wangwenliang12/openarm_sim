# openarm_sim_cpp 代码讲解文档

## 1. 项目是做什么的

这个项目本质上是在做两件事：

1. 用 MuJoCo 跑一个双臂 OpenArm 机器人的仿真。
2. 用 C++ 写控制逻辑，让机器人完成从待机到抓取物体再抬起的闭环任务。

项目里实际上有两套控制路径：

- `openarm_core.so`
  这是一个较基础的控制库，前 3 秒做关节空间 PD，之后切到笛卡尔阻抗控制，主要用于验证动力学控制器是否工作正常。
- `openarm_closed_loop.so`
  这是更完整的闭环抓取系统，包含状态机、目标物体位姿通信、逆运动学、关节轨迹插值、关节限速和平滑控制。

如果把这个项目看成一个完整系统，可以粗略拆成下面几层：

- 仿真层：MuJoCo 模型、`mjModel`、`mjData`
- 几何/运动学层：Pinocchio，负责 URDF 建模、正运动学、雅可比、逆运动学
- 任务规划层：状态机 + 轨迹规划 + 目标姿态计算
- 通信层：LCM 发布/订阅物体位姿
- 控制执行层：把目标关节角写回 MuJoCo actuator

所以这个项目不是一个单纯的“控制器 demo”，而是一个小型机器人软件栈。

---

## 2. 目录结构怎么理解

### 2.1 核心源码目录

- `include/`
  放头文件，定义类接口。
- `src/`
  放实现文件，是真正的逻辑主体。
- `config/`
  放状态机参数和命名姿态配置。
- `model/`
  放 MuJoCo XML、URDF、网格模型。
- `lcm_types/`
  放 LCM 消息定义。
- `python_runner.py`
  启动基础控制库 `openarm_core.so`。
- `python_closed_loop_runner.py`
  启动完整闭环抓取库 `openarm_closed_loop.so`。

### 2.2 从学习顺序看，建议按这个顺序读

1. `python_closed_loop_runner.py`
2. `src/closed_loop_interface.cpp`
3. `src/runtime/closed_loop_runtime.cpp`
4. `src/state_machine/grasp_state_machine.cpp`
5. `src/task/manipulate_task.cpp`
6. `src/robot/robot_kinematics.cpp`
7. `src/task/joint_trajectory_planner.cpp`
8. `src/publisher/lcm_pose_publisher.cpp` 和 `src/subscriber/lcm_pose_subscriber.cpp`

因为这是系统的主路径。你先理解“怎么跑起来”，再看“每个模块怎么做”会更顺。

---

## 3. 整体运行流程

以闭环抓取版本为例，主流程如下：

1. Python 加载 MuJoCo 模型。
2. Python 用 `ctypes` 加载 C++ 动态库 `openarm_closed_loop.so`。
3. Python 把 `mjModel*` 和 `mjData*` 传给 C++ 的 `sim_init`。
4. `sim_init` 创建 `ClosedLoopRuntime` 对象，并做初始化。
5. MuJoCo 每个控制周期调用一次 C++ 的 `sim_step`。
6. `ClosedLoopRuntime::step()` 在每个周期里做：
   - 发布物体真值位姿
   - 订阅当前观测到的物体位姿
   - 同步机器人关节状态到 Pinocchio
   - 读取末端当前位姿
   - 更新抓取状态机
   - 根据状态机命令生成关节目标
   - 对目标关节做平滑和速度限制
   - 加重力补偿
   - 把目标写到 MuJoCo 的 actuator 控制量

这是一条非常标准的机器人闭环链路：

`仿真状态 -> 感知/通信 -> 决策 -> 运动学求解 -> 控制输出`

### 为什么这么分层

因为机器人系统最怕“所有逻辑写在一个大循环里”。

如果状态机、逆解、控制、通信都混在一起：

- 很难调试
- 很难替换单个模块
- 很难知道 bug 是出在感知、规划还是控制

这个项目虽然不算大，但已经用了比较合理的分层方法：

- `GraspStateMachine` 只负责“下一步想做什么”
- `ManipulateTask` 只负责“把意图变成关节目标”
- `RobotKinematics` 只负责“几何和逆运动学”
- `ClosedLoopRuntime` 只负责“把整个系统串起来”

这就是比较典型的工程化写法。

---

## 4. 构建系统：`CMakeLists.txt`

这个文件定义了项目怎么编译。

### 做了什么

- 设置 C++17
- 查找 `Pinocchio`
- 动态定位 Python 安装的 MuJoCo 包路径
- 从 MuJoCo Python 包里找到头文件和运行时库
- 构建两个共享库：
  - `openarm_core`
  - `openarm_closed_loop`
- 如果系统安装了 LCM，就额外生成 LCM 消息头文件并编译闭环模块

### 为什么这样做

#### 1. 通过 Python 包路径找 MuJoCo

项目不是假设 MuJoCo 被系统级安装，而是直接问当前 `python3`：

- 你的 `mujoco` 包装在哪里？

这样做的好处是：

- 和 Python 运行环境对齐
- 不容易出现“Python 能导入 mujoco，但 CMake 找不到动态库”的问题

#### 2. 把 LCM 做成可选项

`OPENARM_BUILD_LCM_TOOLS` 默认开，但如果你环境里没有 LCM，也不会让整个项目完全不可编译。

这是一种典型的降级设计：

- 核心控制库尽量能独立工作
- 增强功能按条件启用

这样方便开发和迁移环境。

---

## 5. 闭环系统最核心的类：`ClosedLoopRuntime`

文件：

- `include/runtime/closed_loop_runtime.hpp`
- `src/runtime/closed_loop_runtime.cpp`

这个类是整个闭环抓取系统的总调度器。

### 5.1 这个类负责什么

它把下面几个模块串起来：

- `ManipulateTask task_`
- `LcmPosePublisher publisher_`
- `LcmPoseSubscriber subscriber_`
- `GraspStateMachine state_machine_`

并负责和 MuJoCo 直接交互：

- 读取 `qpos` / `qvel`
- 写入 `ctrl`
- 写入 `qfrc_applied`

可以把它理解成系统主循环的“编排器”。

### 5.2 成员解释

#### `MjJointMap`

这是一个私有结构体，用来记录“同一个关节在不同系统中的编号映射”：

- `name`
  关节名字
- `mj_joint_id`
  MuJoCo joint id
- `mj_qpos_adr`
  MuJoCo 在 `qpos` 里的地址
- `mj_dof_adr`
  MuJoCo 在 `qvel` / 力维度里的地址
- `pin_q_adr`
  Pinocchio 的位置索引
- `pin_v_adr`
  Pinocchio 的速度索引
- `actuator_id`
  MuJoCo actuator id

### 为什么要有这个映射结构

因为 MuJoCo 和 Pinocchio 都维护自己的关节索引体系：

- 名字一样
- 下标不一定一样

如果每次循环都临时查名字、猜索引，会非常危险，也很慢。

所以初始化时一次性建立映射，后面每一帧直接用，是标准做法。

### 5.3 构造函数

构造时主要做四件事：

1. 创建 `ManipulateTask`
2. 创建 LCM 发布/订阅器
3. 创建 `GraspStateMachine`
4. 建立 MuJoCo 和 Pinocchio 的关节映射

还会设置一个硬编码的 `home_q`。

### 为什么这里硬编码 home 姿态

因为系统后面有很多逻辑默认依赖一个稳定起始姿态：

- 左臂被锁在 home
- 右臂从 home 出发抓取
- 状态机中的 `idle` 姿态需要和实际目标一致

如果没有统一 home 参考，逆解初值、状态切换、命名姿态到达判断都会变得不稳定。

### 5.4 `initialize`

做三件关键事：

1. 先把 MuJoCo 当前状态同步到 `ManipulateTask`
2. 用当前末端位置初始化状态机
3. 从状态机配置里加载命名姿态，再设置平滑命令初值

### 为什么初始化顺序是这样

因为状态机、运动学和命名姿态都依赖“当前机器人状态”。

如果不先同步真实关节角：

- `idle` 姿态的基准会错
- 平滑器初值会错
- IK 初值也会错

这样一上电可能就会跳变。

### 5.5 `step`

这是整个系统最重要的函数。

每个控制周期执行一次，顺序非常值得学习：

1. `publisher_.publishTruthPoses(model, data);`
2. `subscriber_.poll();`
3. `syncRobotStateToTask(data);`
4. 计算当前末端位置
5. 判断 `idle` 姿态是否到达
6. 从订阅器里取目标物体位姿
7. 调用状态机 `update`
8. 如果状态变了，再调用一次 `update` 取新状态命令
9. 通知任务层清理旧规划或进入 `Done hold`
10. 根据命令确保当前状态拥有轨迹
11. 采样关节目标
12. 对关节目标限速平滑
13. 重力补偿
14. 写关节、夹爪、头部控制

### 为什么这个顺序合理

#### 先发布再订阅

这里更像是一个仿真内自发自收的闭环测试环境：

- 发布器从 MuJoCo 读真值
- 订阅器再读回消息

这样做的目的是把“感知输入接口”工程化，而不是把物体位置直接硬编码到状态机里。

以后要替换成外部视觉系统时，只要继续发同样的 LCM 消息，后面的状态机和任务层就不用改。

#### 先同步机器人状态，再算控制

因为所有后续模块都依赖当前机器人状态：

- 末端位姿靠正运动学
- IK 要用当前关节做种子
- 平滑限速也要对比当前关节

#### 状态机和任务层分离

状态机只输出“要去哪”和“夹爪开合多少”。

它不直接管：

- 怎么求逆解
- 怎么插值
- 怎么限速

这样职责更单一。

#### 状态切换后立刻二次 `update`

这是当前实现里一个很关键的小细节。

第一次 `update` 可能在函数内部触发状态切换，但返回的仍然是“旧状态生成的命令”。

所以 runtime 在检测到 `current_state != last_state_` 后，会立刻再调一次 `update`，拿到新状态真正应该执行的命令，再交给任务层。

### 5.6 `syncRobotStateToTask`

把 MuJoCo 的 `qpos/qvel` 根据映射表拷贝成 Pinocchio 使用的 `q/dq`。

### 为什么不直接共享内存

因为两边数据布局不是天然一致的：

- 关节顺序可能不同
- 位置和速度索引规则也不同

显式映射虽然麻烦一点，但最稳。

### 5.7 `controlLimitsForState`

根据状态机状态调整控制速度限制：

- `Descend / Close / Lift` 更快
- 其他状态更慢

### 为什么按状态调速

因为不同阶段控制目标不同：

- `Home / Pregrasp` 更重视平稳
- `Descend` 需要更利落
- `Close` 夹爪动作要更快，不然容易错过抓取时机

这是典型的任务相关速度调度。

### 5.8 `applyRobotGravityCompensation`

把 MuJoCo 已经算好的 `qfrc_bias` 直接写进 `qfrc_applied`。

### 为什么这样做

这里的控制输出主要是位置目标而不是完整动力学力矩控制。

如果不给重力补偿：

- 机械臂会下垂
- 位置伺服误差会变大
- 抓取时动作会更不稳定

既然 MuJoCo 已经能准确算出 bias force，直接利用是最简单可靠的方案。

### 5.9 `writeArmPositionControls`

把 `q_cmd` 写到对应 actuator 的 `data->ctrl`。

这说明当前 actuator 的工作模式更接近位置控制输入，而不是纯力矩控制。

### 为什么抓取闭环选择“位置目标 + 重力补偿”

因为对于抓取任务：

- 它通常比纯力矩控制更稳
- 调参更容易
- 和状态机、IK、轨迹插值更自然地配合

如果直接上全动力学任务空间控制，系统复杂度和调试成本都更高。

### 5.10 `writeFingerControls`

把 `[0,1]` 的夹爪指令映射为实际关节目标距离，比例是 `0.044`。

### 为什么要单独处理夹爪

夹爪和机械臂大关节不一样：

- 运动范围小
- 执行目标通常是“开/合程度”
- 不需要完整 IK

所以把它们从手臂大关节控制逻辑里拆开是合理的。

### 5.11 `writeHeadControls`

头部执行器固定写 0。

### 为什么存在这个函数

因为模型里有头部自由度，但当前任务并不想控制头部。

单独写一个函数把头部锁住，有两个好处：

- 防止无关自由度漂移
- 保持主控制流程结构完整

---

## 6. 任务层核心类：`ManipulateTask`

文件：

- `include/task/manipulate_task.hpp`
- `src/task/manipulate_task.cpp`

这个类的职责是：

- 接收状态机命令
- 把命令转成规划请求
- 管理多个运动规划器和轨迹执行器
- 对目标关节做平滑限速
- 在 `Done` 状态锁定最后一帧关节角

它是“决策层”和“运动学层”之间的桥梁。

### 6.1 成员解释

- `RobotKinematics kinematics_`
  负责 FK / IK
- `interpolation_planner_`
  负责关节空间插值规划
- `ompl_planner_`
  负责笛卡尔目标对应的 OMPL 规划
- `TrajectoryExecutor executor_`
  负责按时间采样当前状态的轨迹
- `named_pose_targets_`
  保存 `idle` 这类命名姿态对应的关节向量
- `hold_q_ / hold_position_active_`
  用于 `Done` 状态锁住最后一帧关节角
- `smoothed_q_cmd_`
  保存上一个平滑后的关节命令，用于做限速

### 为什么要单独有 `ManipulateTask`

如果把这些逻辑直接塞进 `ClosedLoopRuntime`：

- runtime 会变成“什么都做”的巨类
- IK、规划、平滑、姿态管理会互相缠绕

而 `ManipulateTask` 的存在让系统层次清楚很多：

- runtime 管系统流程
- task 管机器人动作生成

### 6.2 `loadNamedPoses`

把 JSON 配置里的命名姿态转换成完整关节向量。

### 为什么要转成完整关节向量

因为配置文件通常只写“关心的关节”，而控制时需要的是完整 `q_target`。

预先展开成完整向量的好处是：

- 运行时少做字符串查找
- 比较是否到达目标时更方便
- 后面写到 actuator 时更直接

### 6.3 `isNamedPoseReached`

判断当前关节是否已经足够接近某个命名姿态。

### 为什么要有这个判断

状态机从 `Home` 离开不是只看时间，还要看是否已经基本到位。

否则会出现：

- 还没回到稳定起始姿态，就开始抓
- 后续逆解初值不可靠

这会让动作很乱。

### 6.4 `buildMotionRequest`

把状态机命令转换成统一的 `MotionRequest`。

核心内容包括：

- 记录 `owner_state_id`
- 把当前关节角作为 `start_q`
- 对 `Descend / Lift` 提高关节速度上限
- 根据命令模式填充 `joint_goal_q` 或 `target_pos / target_quat`

### 为什么要这样分

状态机只表达意图，任务层负责把意图翻译成规划器能理解的统一输入。

这样后面不管换插值规划器还是 OMPL，调用接口都一致。

### 6.5 `ensureTrajectoryForState`

保证某个状态进入后拥有属于自己的轨迹。

逻辑是：

- 如果该命令不需要规划，直接返回
- 如果当前已经有同一状态的活动轨迹，继续复用
- 否则创建新的 `MotionRequest`
- 根据请求类型选择 `InterpolationMotionPlanner` 或 `OmplMotionPlanner`
- 创建 `PlannedTrajectory` 后交给 `TrajectoryExecutor`

### 为什么规划要和状态绑定

因为轨迹计划是“某个状态下的运动意图”。

如果状态变了，旧计划继续执行就不合理了。比如：

- 还在执行 `Pregrasp` 的插值
- 但状态已经切到 `Descend`

这时继续沿用旧计划会把动作搞乱。

所以计划有 `owner_state_id`，这是一个很好的保护设计。

### 6.6 `onStateTransition`

状态切换时清掉旧轨迹；如果切到 `Done`，额外锁住当前关节角。

### 为什么要主动清理

因为执行器内部持有“上一状态的轨迹”。

状态切换时不清理，最容易出现“跨状态复用旧轨迹”的 bug。

而 `Done` 是一个特殊状态：

- 不再生成新轨迹
- 也不应该退化成“跟随当前姿态”
- 应该保持切入 `Done` 那一刻的关节角不动

### 6.7 `smoothJointTargets`

这是非常实用的一个函数。

它不是直接把 `q_target` 发给控制器，而是做每步最大变化限制：

- 大关节按 `arm_joint_velocity * dt`
- 手指按 `finger_velocity * dt`

### 为什么必须做平滑限速

因为轨迹采样、IK 或命名姿态输出的目标经常是“理论上对的”，但不一定适合直接执行。

如果不做限速：

- 相邻两帧关节目标可能跳变
- 位置伺服会过激
- 抓取时容易抖动

所以这里等于给整套系统加了一个低成本的执行器保护层。

### 6.8 `setJointAngles`

把平滑器内部状态直接设置为某个关节向量。

### 为什么需要它

初始化时要避免第一帧把机器人从零向量“拉”到真实状态。

所以应该把平滑器起点设成当前真实关节角，这样控制才连续。

### 6.9 `sampleJointTargets`

从执行器采样当前应执行的关节角。

规则很直接：

- 如果有活动轨迹，就按当前时间插值采样
- 如果没有活动轨迹，但 `Done hold` 已激活，就返回锁定的 `hold_q_`
- 否则返回当前实际关节角

### 为什么 `Done hold` 必须放在这里

因为这里是“最终关节目标输出口”。

把 hold 逻辑放在这里有两个好处：

- 不需要给状态机增加关节级别细节
- 不会影响 `Home / Pregrasp / Descend / Lift` 的正常规划流程

---

## 7. 运动规划与执行层

文件：

- `include/task/motion_request.hpp`
- `include/task/planned_trajectory.hpp`
- `include/task/motion_planner.hpp`
- `include/task/interpolation_motion_planner.hpp`
- `src/task/interpolation_motion_planner.cpp`
- `include/task/ompl_motion_planner.hpp`
- `src/task/ompl_motion_planner.cpp`
- `include/task/trajectory_executor.hpp`
- `src/task/trajectory_executor.cpp`

现在这一层不再是旧的 `JointTrajectoryPlanner`，而是拆成了四个更清楚的部件：

- `MotionRequest`
  描述“我要从哪里到哪里，用什么目标类型”
- `PlannedTrajectory`
  描述“规划器给出的时间参数化关节路径”
- `IMotionPlanner`
  统一规划接口
- `TrajectoryExecutor`
  负责执行期采样

### 为什么要拆成这些小部件

因为“请求”、“规划结果”和“执行状态”不是一回事。

拆开之后：

- `ManipulateTask` 负责组装请求
- 规划器只负责产出轨迹
- 执行器只负责按时间采样

职责更干净，后面替换某个规划器也更容易。

### 7.1 `MotionRequest`

它包含：

- `owner_state_id`
- `goal_type`
- `start_q`
- `joint_goal_q`
- `target_pos / target_quat`
- 规划时间和速度约束

### 为什么请求里同时支持关节目标和笛卡尔目标

因为 `Home` 这种命名姿态和 `Pregrasp / Descend / Lift` 这种末端位姿目标，本来就是两类不同任务。

统一到一个结构之后，任务层切规划器时就不用分叉太多逻辑。

### 7.2 `PlannedTrajectory`

它包含：

- `valid`
- `owner_state_id`
- `duration`
- 一组 `TrajectoryPoint`

### 为什么结果只保存关节轨迹

因为最终写进 MuJoCo actuator 的本来就是关节位置命令。

不管前面目标是关节空间还是笛卡尔空间，规划完成以后都统一落到关节轨迹，这样执行层最简单。

### 7.3 `InterpolationMotionPlanner`

这是最轻量的规划器。

做法是：

- 先把请求解析成目标关节角
- 再用 smoothstep 在 `start_q` 和 `goal_q` 之间生成固定段数插值
- 根据最大关节差和速度上限估算总时长

### 为什么还保留插值规划器

因为 `JointGoal` 本来就不需要搜索路径。

而且就算是笛卡尔目标，在没有 OMPL 或者问题很简单时，插值 + IK 也能作为一个廉价后备方案。

### 7.4 `OmplMotionPlanner`

这个规划器在启用了 OMPL 时：

- 先把 `start_q / goal_q` 裁成右臂关节子空间
- 用 `RRTConnect` 在右臂关节空间搜索
- 对解路径做简化
- 再拼回完整机器人关节向量
- 最后重新时间参数化成 `PlannedTrajectory`

如果没有编译进 OMPL，就退化为空规划结果。

### 为什么 OMPL 只规划右臂

因为当前抓取任务只有右臂在主动操作目标。

把左臂固定住，搜索维度更低，路径规划会更稳定也更快。

### 7.5 `TrajectoryExecutor`

执行器持有一条已经生成的轨迹，并提供：

- `setTrajectory`
- `clear`
- `hasActiveTrajectory`
- `ownerStateId`
- `sample`

### 为什么执行器要单独存在

因为规划和执行是两个阶段：

- 规划只在状态进入时触发一次
- 执行要在每个控制周期里持续按时间采样

单独拆出来以后，状态切换时清理旧轨迹也更直接。

---

## 8. 运动学核心类：`RobotKinematics`

文件：

- `include/robot/robot_kinematics.hpp`
- `src/robot/robot_kinematics.cpp`

这个类负责：

- 持有 Pinocchio 模型
- 保存当前机器人状态
- 计算末端位姿
- 进行逆运动学求解

它是整个任务生成最关键的数学模块。

### 8.1 `JointInfo`

这是一个关节描述结构体，包含：

- `name`
- `pin_q_adr`
- `pin_v_adr`
- 关节上下限
- 是否是手指
- 属于左臂、右臂还是其他组
- `home_fixed`

### 为什么需要 `JointInfo`

因为这个项目不是单臂，而是双臂 + 手指。

很多控制逻辑都需要知道：

- 这个索引对应哪个关节
- 它是不是手指
- 它属于左臂还是右臂

所以不能只用裸索引，必须有语义化描述。

### 8.2 构造函数

构造时做这些事：

1. 从 URDF 建 Pinocchio 模型
2. 找末端 frame id
3. 初始化当前关节、速度和 home 状态
4. 遍历左右臂关节名，构建 `JointInfo`
5. 记录左右臂非手指关节索引

### 为什么把左臂和右臂单独分组

因为这个项目的抓取动作实际上是右臂在工作，左臂更多像固定支撑。

所以后续 IK 里会看到：

- 左臂关节被锁到 home
- 只让右臂参与逆解

这就是分组信息的核心用途。

### 8.3 `setRobotState`

更新当前 `q_robot_` 和 `dq_robot_`。

### 为什么 `RobotKinematics` 自己保存状态

因为 IK、FK 和命名姿态判断经常都要用“当前关节状态”。

如果每次调用都从外面传：

- 接口会变得很啰嗦
- 很多地方容易传错版本

保存为内部状态更适合这种周期循环式控制代码。

### 8.4 `getEndEffectorPose`

用 Pinocchio 正运动学求当前末端位姿。

### 为什么末端位姿要通过 Pinocchio 算

因为状态机的很多判断都在笛卡尔空间进行：

- 离预抓取点还有多远
- 是否已经下降到目标高度
- 是否已经抬起

这些不能只看关节角。

### 8.5 `solveIK`

这是这个类最重要的函数。

它的逆解策略可以概括成：

1. 用 `seed_q` 作为初值
2. 把左臂关节强制固定在 `home_q`
3. 迭代最多 80 次
4. 每次计算当前末端误差
5. 取右臂部分雅可比
6. 用阻尼伪逆求解
7. 再加一个零空间项，让结果不要偏离 seed 太远
8. 更新关节并裁剪到上下限

### 为什么这样做

#### 1. 固定左臂，只动右臂

因为当前任务只需要右臂抓取。

如果双臂都一起参与 IK：

- 解空间更大
- 动作更难预测
- 调试更复杂

固定左臂能显著降低问题复杂度。

#### 2. 使用阻尼伪逆

标准雅可比伪逆在奇异位形附近容易数值不稳定。

加入阻尼项：

`J J^T + lambda I`

能让求解更稳。

#### 3. 加零空间 seed bias

这一步很关键：

- 主任务满足末端误差
- 零空间项尽量让解靠近种子姿态

这样可以避免 IK 在多解情况下跳到很奇怪的姿势。

#### 4. 每轮做关节限位裁剪

如果不裁剪，迭代很容易走到 URDF 定义范围外，后面控制就会不可信。

### 8.6 右臂相关辅助接口

当前 `RobotKinematics` 还提供了一组专门给规划器用的辅助函数：

- `extractRightArmQ`
- `mergeRightArmQ`
- `rightArmLowerBounds`
- `rightArmUpperBounds`
- `rightArmDof`
- `solveRightArmIK`

### 为什么这些接口很重要

因为当前抓取规划只在右臂子空间里展开。

规划器需要：

- 从完整关节向量里提取右臂变量
- 把右臂规划结果再合并回完整机器人状态
- 读取右臂的搜索边界

这些逻辑如果散落在规划器里，会很乱。

### 8.7 `homeQ`

保存 home 姿态。

### 为什么 IK 要依赖 home 姿态

因为这里的 home 不只是“默认姿态”，它还承担了约束左臂不乱动的作用。

所以它其实是任务先验的一部分。

---

## 9. 抓取决策类：`GraspStateMachine`

文件：

- `include/state_machine/grasp_state_machine.hpp`
- `src/state_machine/grasp_state_machine.cpp`

这个类负责决定“当前任务应该处于哪个阶段”，并输出该阶段对应的命令。

### 9.1 状态定义

- `Home`
  回到待机姿态，夹爪张开
- `Pregrasp`
  先移动到物体上方
- `Descend`
  向物体下探
- `Close`
  合拢夹爪
- `Lift`
  抬起物体
- `Done`
  保持完成状态

### 为什么状态这样划分

这是非常典型的抓取任务分解：

- 回到稳定起点
- 靠近目标
- 接触前下探
- 夹紧
- 抬起验证

这样拆的好处是每个阶段目标清楚、条件简单，也容易调参数。

### 9.2 `CommandMode`

- `JointPose`
- `CartesianPose`

### 为什么同时支持两种命令模式

因为不是所有动作都适合在笛卡尔空间表达。

例如：

- `Home` 更适合用固定关节姿态
- `Pregrasp / Descend / Lift` 更适合用末端目标位姿

把两类命令统一封装到一个 `Command` 里，就能让上层 runtime 用统一接口处理。

### 9.3 `Command`

状态机输出命令结构，包含：

- 模式
- 命名关节姿态名
- 目标末端位置
- 目标末端姿态
- 左右夹爪开合命令
- 是否要求任务层为当前状态规划机械臂轨迹

### 为什么状态机只输出命令，不直接控制机器人

因为状态机的职责应该是“决策”，不是“求解和执行”。

如果状态机直接写关节角、直接操作 MuJoCo：

- 它会同时负责决策、规划、控制
- 很难维护

现在的设计更合理：状态机只说“我要去哪里”。

### 9.4 `Config`

配置结构中包括：

- 是否只停在 home
- 各阶段持续时间
- 预抓取、下探、抬升高度
- 各阶段位置容差
- 命名姿态

### 为什么把这些参数放配置文件

因为这些值非常容易调，而且强依赖任务场景。

如果全部写死在 C++：

- 每调一次参数就要重新编译
- 场景迁移不方便

放 JSON 是更工程化的方式。

### 9.5 `initialize`

加载配置，设置起始状态，并记录当前末端位置。

### 为什么初始化时记录当前位姿

因为状态机后续很多过渡判断都基于当前末端位置。

如果没有可靠起点，一开始的 `Pregrasp` 参考就不稳定。

### 9.6 `update`

根据：

- 当前时间
- 当前末端位置
- 观测到的物体位姿
- 当前是否已经到达命名姿态

输出下一条命令。

### 为什么 `update` 接口要传这些量

因为状态机本质上是一个“依赖观测输入的离散决策器”。

它至少需要知道：

- 现在在哪
- 目标在哪
- 上一个阶段是否完成
- 时间是否满足切换条件

### 9.7 `loadConfig`

从 JSON 解析：

- `timing`
- `cartesian_targets`
- `poses`

具体会读到：

- `home_only_mode`
- `home_duration_sec`
- `close_duration_sec`
- `pregrasp_height`
- `descend_height`
- `lift_height`
- `pregrasp_tolerance`
- `descend_tolerance`
- `lift_tolerance`
- 各个命名姿态里的左右臂关节值

### 为什么状态机自己加载配置

因为这些配置完全属于状态机语义本身。

如果交给 runtime 解析再灌进来，反而会让 runtime 知道太多状态机细节。

### 9.8 `transitionTo`

切换状态时做附带处理：

- 到 `Pregrasp` 时重置起始标记
- 到 `Descend` 时锁定抓取目标位姿
- 回到 `Home` 时释放锁定

### 为什么 `Descend` 时要锁定目标位姿

这是这个状态机里很重要的一个设计。

如果下降过程中物体观测还在实时更新，目标点就可能抖动。

结果会是：

- 末端在接近物体时左右乱修正
- 降低抓取稳定性

所以进入 `Descend` 时，把目标物体姿态冻结下来，下降过程就更稳定。

### 9.9 `handleHome`

输出命名姿态 `idle`，左右夹爪都张开。

达到条件后切到 `Pregrasp`。

### 为什么 `Home` 同时看时间和是否到位

只看时间不够，因为机器人可能还没走到稳定姿态。

只看到位也不够，因为可能一开始就刚好接近目标，需要给系统一点稳定时间。

两者结合更稳。

### 9.10 `handlePregrasp`

把末端目标设为“物体上方一定高度”，姿态用顶部抓取姿态，夹爪打开。

### 为什么先到上方再下降

这是一种经典抓取策略，原因很实际：

- 避开桌面和物体侧面碰撞
- 路径更简单
- 后续下降更容易控制

### 9.11 `makeTopDownGraspQuat`

根据物体姿态生成一个“自上而下抓取”的末端姿态：

- 提取物体 x 轴方向
- 算 yaw
- 再叠加一个绕 y 轴 180 度的工具对齐旋转

### 为什么这么做

因为抓取器不是任意方向都能抓。

这里的目标是：

- 保持从上往下接近
- 同时让夹爪 yaw 跟物体朝向对齐

这样更接近真实抓取需求。

### 9.12 `handleDescend`

目标设为物体位置上方很小的 `descend_height`。

### 为什么不直接下降到物体中心

因为物体中心通常不是接触安全点。

给一个略高的偏移更合理，可以减少模型误差或碰撞误差造成的穿透。

### 9.13 `handleClose`

保持末端位姿不动，让右夹爪关闭。

### 为什么关闭夹爪时保持位姿

抓取成功率很大程度依赖闭合过程中的末端稳定性。

如果夹爪在闭合时末端还在移动，容易把物体推走。

### 9.14 `handleLift`

闭合后把物体向上抬到 `lift_height`。

### 为什么 Lift 是必要阶段

因为抓住不等于任务完成。

真正的抓取验证往往是：

- 夹住之后还能把物体带离桌面

所以 lift 本质上是一个成功验证阶段。

### 9.15 `handleDone`

保持完成状态，不再切换，并继续输出“右夹爪闭合”的语义命令。

### 为什么要有 Done 状态

如果没有终止态，状态机会持续尝试重复抓取或重新规划。

对于 demo 和调试，这样不利于观察最终结果。

另外在当前实现里，真正“保持不动”不是由状态机自己完成的，而是由 `ManipulateTask` 在切入 `Done` 时锁住最后一帧关节角。

也就是说：

- 状态机负责说“任务结束了”
- 任务层负责说“结束后就保持这组关节角不要再动”

---

## 10. 通信层：`LcmPosePublisher`

文件：

- `include/publisher/lcm_pose_publisher.hpp`
- `src/publisher/lcm_pose_publisher.cpp`

### 10.1 作用

从 MuJoCo 真值中读取几个彩色方块的位姿，并通过 LCM 发布出去。

### 10.2 关键成员

- `channel_`
  LCM 通道名
- `lcm_`
  LCM 对象
- `red_block_body_id_ / yellow_block_body_id_ / blue_block_body_id_`
  对应 MuJoCo body id

### 10.3 `initialize`

根据 body 名称找到三个方块的 id。

### 为什么初始化时查 body id

和关节映射的思路一样：

- 名字只查一次
- 运行时直接按 id 读数组

### 10.4 `publishTruthPoses`

发布一个 `ObjectPoseArray`，里面有三个物体位姿。

### 为什么即使在仿真里也要“发布消息”

因为系统设计上想模拟真实机器人里的感知链路：

- 视觉模块发布物体位姿
- 控制模块订阅物体位姿

这样以后接入真实视觉时，控制代码可以少改很多。

### 10.5 `fillPoseMessage`

从 `data->xpos` 和 `data->xquat` 填充 LCM 消息。

### 为什么发布的是 body 世界位姿

因为状态机关心的是“物体在世界里的位置和朝向”，这正是抓取任务最直接需要的信息。

---

## 11. 通信层：`LcmPoseSubscriber`

文件：

- `include/subscriber/lcm_pose_subscriber.hpp`
- `src/subscriber/lcm_pose_subscriber.cpp`

### 11.1 作用

订阅 `ObjectPoseArray`，缓存最新的物体位姿。

### 11.2 成员解释

- `channel_`
  订阅通道
- `lcm_`
  LCM 实例
- `poses_`
  最近一次接收到的物体位姿缓存

### 为什么要做缓存

因为控制循环每一帧只需要“最新位姿”，并不需要自己处理消息队列历史。

缓存式设计更简单，也符合控制器使用方式。

### 11.3 `poll`

非阻塞轮询 LCM。

### 为什么用 `handleTimeout(0)`

因为控制循环不能被消息接收阻塞。

机器人控制必须优先保证周期执行，所以这里只能做轻量轮询。

### 11.4 `getPose`

按 `object_id` 读取缓存。

### 为什么用对象 id 查找

因为状态机只关心某一个目标物体，例如 `red_block`。

缓存成 `unordered_map<string, ObjectPose>` 最符合这个需求。

### 11.5 `handleMessage`

收到一批消息后更新缓存，并把四元数归一化。

### 为什么收到后要归一化四元数

四元数在消息传输、浮点误差下可能稍微偏离单位长度。

如果直接拿去做姿态运算：

- 旋转误差会变脏
- 角轴转换可能不稳定

所以归一化是个好习惯。

---

## 12. 数据结构：`ObjectPose`

文件：

- `include/common/object_pose.hpp`

### 12.1 字段

- `utime`
- `position`
- `quaternion`

### 为什么单独定义这个结构

因为项目内部不想在所有模块里都直接依赖 LCM 自动生成类型。

自己定义一个内部通用结构有几个好处：

- 任务层和状态机不需要知道 LCM 细节
- 更容易替换通信方式
- 数据语义更清楚

这是一个典型的“通信消息类型”和“内部业务类型”分离。

---

## 13. 基础动力学控制类：`CartesianImpedanceController`

文件：

- `include/robot/cartesian_impedance_controller.hpp`
- `src/cartesian_impedance_controller.cpp`

这个类属于另一条控制路径，对应 `openarm_core.so`。

它实现了一个任务空间阻抗控制器。

### 13.1 作用

输入：

- 当前关节角 `q`
- 当前关节速度 `dq`
- 目标末端位置
- 目标末端姿态

输出：

- 关节力矩 `tau`

### 为什么这个类存在

闭环抓取版本主要用位置目标控制，而这个类展示的是更底层的动力学控制能力：

- 先在任务空间算期望加速度
- 再映射成关节加速度
- 最后通过动力学模型转成力矩

它更像是一个控制算法验证模块。

### 13.2 成员解释

- `model_`, `data_`
  Pinocchio 模型和缓存
- `ee_frame_id_`
  末端 frame id
- `Kp_`, `Kd_`
  6 维阻抗增益
- `M_`
  质量矩阵
- `Minv_`
  质量矩阵逆
- `nle_`
  非线性项，通常含科氏、离心、重力
- `J_`
  末端雅可比
- `A_`
  任务空间惯性相关矩阵

### 13.3 构造函数

从 URDF 建模、找到末端 frame、初始化默认增益和缓存矩阵。

### 为什么缓存矩阵做成员变量

因为控制循环会反复分配这些矩阵。

提前按维度分配可以减少频繁动态内存开销，也更清楚表达算法会用到哪些量。

### 13.4 `setGains`

设置位置和旋转方向的刚度/阻尼。

### 为什么位置和姿态增益分开

因为末端平移和转动的尺度完全不同。

如果用统一增益：

- 平移可能太软
- 旋转可能太激进

分开是阻抗控制的常见做法。

### 13.5 `getCurrentPose`

做 FK 并返回当前末端位姿。

### 13.6 `computeTorque`

这是核心算法，步骤大致是：

1. `computeAllTerms` 计算动力学项
2. 更新 frame 位姿
3. 取质量矩阵 `M` 和非线性项 `nle`
4. 取末端雅可比 `J`
5. 计算当前位置/姿态误差
6. 计算末端当前速度
7. 根据阻抗律得到期望笛卡尔加速度
8. 用 `A = J M^{-1} J^T` 建立任务空间映射
9. 在接近奇异位形时加阻尼
10. 解出关节加速度
11. 返回 `tau = M qdd + nle`

### 为什么要这样算，而不是简单 `J^T * error`

简单的 `J^T` 形式更像静态映射或简化控制。

这里使用动力学一致的形式，有几个目的：

- 更接近真实任务空间控制
- 能结合惯性矩阵
- 输出直接就是关节力矩

这对高性能控制更有意义。

### 13.7 为什么有奇异性阻尼

当 `A = J M^{-1} J^T` 接近奇异时，直接求逆会不稳定。

代码里通过 manipulability 判断是否接近奇异，然后加阻尼：

- 可操作度低时增大 `lambda`
- 让求解更稳

这是很经典的数值稳定处理。

### 13.8 注意点

代码里计算了 `a_drift_local` 和 `a_drift_world`，但最终 `drift_acc` 实际被设成了 0，没有真正使用漂移项补偿。

这说明这个控制器还不是特别完整，更像实验中的一个可运行版本。

---

## 14. 启动桥接层：`closed_loop_interface.cpp`

文件：

- `src/closed_loop_interface.cpp`

### 做了什么

定义了三个 C 接口：

- `sim_init`
- `sim_step`
- `sim_close`

内部持有一个全局 `std::unique_ptr<ClosedLoopRuntime>`。

### 为什么要用 `extern "C"`

因为 Python 通过 `ctypes` 加载动态库时，更容易直接调用 C ABI 导出的符号。

如果直接导出 C++ 函数名，会遇到 name mangling 问题。

### 为什么用全局 `unique_ptr`

因为 MuJoCo 的控制回调接口本身就是全局式的：

- 初始化一次
- 每帧调用一次

用一个全局 runtime 是最直接的桥接方式。

---

## 15. 基础控制入口：`main_interface.cpp`

文件：

- `src/main_interface.cpp`

这个文件对应 `openarm_core.so` 的入口。

### 做了什么

- 初始化一个 `CartesianImpedanceController`
- 前 3 秒用关节空间 PD 把机器人摆到一个舒适姿态
- 3 秒后切换到笛卡尔阻抗控制
- 在末端 Y-Z 平面画圆
- 把输出力矩直接写入 MuJoCo 控制量

### 为什么前面要先来一段关节 PD

因为阻抗控制通常需要一个比较合理的初始位姿，否则一开始可能误差过大。

先用简单 PD 把姿态摆正，再切到任务空间控制，更容易稳定启动。

### 为什么这里直接写力矩，而闭环抓取版本写位置目标

因为两条路径的目的不同：

- `openarm_core` 更偏算法验证
- `openarm_closed_loop` 更偏任务执行稳定性

所以控制方式也不同。

---

## 16. Python 启动脚本

### 16.1 `python_closed_loop_runner.py`

这是最重要的启动脚本。

### 做了什么

- 加载 `openarm_closed_loop.so`
- 加载 `model/openarm_head.xml`
- 检查关键关节是否存在
- 调用 C++ `sim_init`
- 注册 MuJoCo control callback
- 打开 viewer
- 退出时调用 `sim_close`

### 为什么 Python 负责启动，而不是纯 C++

因为 MuJoCo Python viewer 使用起来很方便：

- 快速可视化
- 调试方便
- 少写 UI/窗口管理代码

而核心控制逻辑仍然放 C++，保留性能和工程结构。

这是一种常见混合架构：

- Python 负责启动、集成、可视化
- C++ 负责核心控制

### 16.2 `python_runner.py`

这是基础版本启动脚本，对应 `openarm_core.so`。

结构和闭环版类似，但模型文件和库文件不同。

---

## 17. 状态机配置：`config/grasp_state_machine.json`

这个文件定义了：

- `home_only_mode`
- `home_duration_sec`
- `close_duration_sec`
- `idle` 姿态
- `pregrasp_height`
- `descend_height`
- `lift_height`
- `pregrasp_tolerance`
- `descend_tolerance`
- `lift_tolerance`

### 为什么这个文件很重要

因为很多“任务行为”其实不在 C++ 逻辑里硬编码，而在这里。

比如：

- 是否只停在 home
- 回家多久后才开始抓
- 抓之前抬高多少
- 下降和抬起判定用多大容差
- 抬起多高算完成

这些参数直接决定动作风格。

学习这个项目时，一定要把它和 `GraspStateMachine` 对照着看。

---

## 18. LCM 消息定义：`lcm_types/openarm_pose.lcm`

定义了两种消息：

- `ObjectPose`
- `ObjectPoseArray`

### 为什么消息设计成数组

因为一帧里通常会一起发布多个物体位姿。

用数组比每个物体单发一条消息更自然，也更容易保证时间一致性。

---

## 19. 这个项目里“为什么这样做”的几个关键设计总结

### 19.1 为什么同时使用 MuJoCo 和 Pinocchio

因为两者擅长的事不同：

- MuJoCo 擅长仿真、接触、执行控制回调
- Pinocchio 擅长 URDF 建模、FK、Jacobian、IK、动力学算法

把两者结合起来，是机器人软件里很常见的组合。

### 19.2 为什么闭环抓取主路径选择“状态机 + IK + 限速位置控制”

因为这是一个优先追求稳定性、清晰性和可调试性的方案。

对于抓取 demo 而言，它通常比直接上复杂力矩控制更容易成功。

### 19.3 为什么要把目标物体位姿经过 LCM 通信，而不是直接读取

因为这相当于提前为真实感知系统留接口。

如果未来接相机识别模块，只要继续发同一种消息，控制侧基本不用改。

### 19.4 为什么只让右臂参与抓取 IK

因为当前任务目标单一，固定左臂能减少解空间，提高可预测性和稳定性。

### 19.5 为什么还要做关节平滑

因为规划器和 IK 给出的目标不一定能直接稳定执行。

平滑器是“理论目标”和“执行器现实能力”之间的一层缓冲。

---

## 20. 学这个项目时最推荐的阅读路径

如果你想真正把代码吃透，我建议你这样看：

### 第一轮：先看系统怎么跑

1. `python_closed_loop_runner.py`
2. `src/closed_loop_interface.cpp`
3. `src/runtime/closed_loop_runtime.cpp`

目标是搞懂：

- 谁在调用谁
- 每帧到底发生了什么

### 第二轮：看任务逻辑

1. `src/state_machine/grasp_state_machine.cpp`
2. `config/grasp_state_machine.json`
3. `src/task/manipulate_task.cpp`

目标是搞懂：

- 状态怎么切
- 每个状态为什么输出这种命令

### 第三轮：看数学部分

1. `src/robot/robot_kinematics.cpp`
2. `src/task/interpolation_motion_planner.cpp`
3. `src/task/ompl_motion_planner.cpp`
4. `src/cartesian_impedance_controller.cpp`

目标是搞懂：

- FK / IK 怎么做
- 插值规划和 OMPL 规划怎么做
- 阻抗控制怎么做

### 第四轮：看通信和配置

1. `src/publisher/lcm_pose_publisher.cpp`
2. `src/subscriber/lcm_pose_subscriber.cpp`
3. `lcm_types/openarm_pose.lcm`

目标是搞懂：

- 感知数据是怎么进入任务系统的

---

## 21. 这个项目的优点

### 21.1 分层清楚

模块边界比较明确，阅读时不容易完全迷路。

### 21.2 工程味道比较足

不是只写一个控制循环，而是已经有：

- 配置文件
- 通信接口
- 状态机
- 任务层
- 运动学层

### 21.3 适合学习“机器人软件系统”而不只是单个算法

它很适合用来理解：

- 仿真和控制怎么结合
- 状态机和 IK 怎么配合
- 感知接口怎么抽象

---

## 22. 这个项目当前也存在的一些局限

### 22.1 感知链路目前是“真值自发自收”

发布器直接从仿真真值读物体位姿，不是真实视觉估计。

这更像接口演练，不是真正感知闭环。

### 22.2 IK 目前是局部迭代法

它简单好用，但不一定总能得到最优或最稳定解。

### 22.3 状态机比较固定

当前抓取流程是硬编码阶段式，不是行为树或更泛化的任务规划框架。

### 22.4 `CartesianImpedanceController` 还有继续完善空间

比如漂移项没有真正用上，说明它更偏实验控制器。

---

## 23. 一句话总结每个类

- `ClosedLoopRuntime`
  系统总调度器，把通信、状态机、任务层和 MuJoCo 串起来。
- `ManipulateTask`
  把状态机命令变成可执行的关节目标。
- `InterpolationMotionPlanner / OmplMotionPlanner / TrajectoryExecutor`
  分别负责生成插值轨迹、生成 OMPL 轨迹，以及按时间执行轨迹。
- `RobotKinematics`
  提供 FK、IK 和机器人关节语义信息。
- `GraspStateMachine`
  定义抓取流程阶段和阶段切换条件。
- `LcmPosePublisher`
  把仿真里的物体真值位姿发布成 LCM 消息。
- `LcmPoseSubscriber`
  接收并缓存最新物体位姿。
- `CartesianImpedanceController`
  用动力学模型实现任务空间阻抗控制。
- `ObjectPose`
  项目内部统一使用的物体位姿数据结构。
- `JointInfo`
  对每个关节的语义描述。

---

## 24. 如果你接下来要继续深入，建议优先做的几件事

1. 在 `ClosedLoopRuntime::step()` 里打印当前状态和目标位置，观察状态切换。
2. 在 `RobotKinematics::solveIK()` 里打印误差收敛过程，理解 IK 行为。
3. 改 `config/grasp_state_machine.json` 里的高度和时间参数，观察动作变化。
4. 对照 MuJoCo XML 中的 actuator 定义，理解 `writeArmPositionControls()` 为什么这么写。
5. 再回头看 `CartesianImpedanceController::computeTorque()`，把任务空间控制链条彻底吃透。

如果你愿意，我下一步可以继续帮你做两件事中的任意一种：

1. 再写一份“按源码文件逐行导读”的文档。
2. 再写一份“从机器人控制原理角度解释这些类”的学习笔记。
