# openarm_sim 关键代码讲解文档

> 本文只讲“关键代码如何工作、类型为什么这样定义、核心逻辑怎么跑”，不展开设计过程。

## 1. 入口与调度：`ClosedLoopRuntime`

关键文件：
- `include/runtime/closed_loop_runtime.hpp`
- `src/runtime/closed_loop_runtime.cpp`

### 1.1 为什么构造函数入参是 `const mjModel*`

`ClosedLoopRuntime::ClosedLoopRuntime(const mjModel* model)` 用的是 MuJoCo 的只读模型指针。

- `mjModel` 保存结构化静态信息（关节、执行器、时间步长等）。
- 构造阶段只需要“读配置、建索引映射”，不需要改模型内容，所以用 `const`。
- 用裸指针而不是拷贝对象，是因为 MuJoCo 模型体积大且由外部生命周期管理（Python + MuJoCo 持有）。

### 1.2 `initialize(const mjModel*, const mjData*)` 为什么 `mjData` 也是 `const`

初始化步骤里只读当前状态，不写控制量：
1. `syncRobotStateToTask(data)` 把 `qpos/qvel` 同步到运动学层。
2. 用当前末端位姿初始化状态机。
3. 加载命名关节姿态。
4. 令平滑命令等于当前关节角，避免第一帧突跳。

因此 `mjData*` 采用 `const` 是正确语义：初始化读取状态，不产生控制副作用。

### 1.3 `step(const mjModel*, mjData*)` 为什么 `mjData*` 非 const

控制循环里必须写仿真控制量：
- `data->ctrl[...]`：位置控制命令。
- `data->qfrc_applied[...]`：重力补偿项。

所以 `step` 的 `mjData*` 必须可写。

### 1.4 `MjJointMap` 结构体为什么包含两套地址

`MjJointMap` 同时保存：
- MuJoCo 索引：`mj_qpos_adr`、`mj_dof_adr`、`actuator_id`
- Pinocchio 索引：`pin_q_adr`、`pin_v_adr`

关键点：MuJoCo 和 Pinocchio 的关节顺序不保证一致。若每帧按名字查找会慢且易错，所以在初始化阶段一次性“名字 -> 双索引地址”固化，实时循环只做 O(n) 数组读写。

### 1.5 `controlLimitsForState()` 返回 `ControlLimits` 的意义

返回值是值类型 `ControlLimits`（两个 `double`）：
- `arm_joint_velocity`
- `finger_velocity`

这样写有两个好处：
- 状态到速度上限的映射逻辑集中在 runtime 层，不污染执行器层。
- 每帧返回一份轻量配置对象，调用方按值接收即可，不涉及共享状态同步问题。

### 1.6 控制写入关键逻辑

1. `applyRobotGravityCompensation`：把 `qfrc_bias` 拷到 `qfrc_applied`，等价于补偿模型重力/科氏等偏置。
2. `writeArmPositionControls`：按关节名筛选“arm joint”并写入位置目标。
3. `writeFingerControls`：夹爪命令先约束到 `[0, 1]`，再缩放到物理开度（`0.044m`）。

这里 `writeFingerControls(mjData*, double left, double right)` 使用 `double` 而不是 `bool`，是因为夹爪不是开/关二值，而是可连续插值控制。

---

## 2. 任务层：`ManipulateTask`

关键文件：
- `include/task/manipulate_task.hpp`
- `src/task/manipulate_task.cpp`

### 2.1 构造参数为什么是路径 + 关节名列表

```cpp
ManipulateTask(const std::string& urdf_path,
               const std::string& ee_frame_name,
               const std::vector<std::string>& right_arm_joints,
               const std::vector<std::string>& left_arm_joints)
```

参数类型含义：
- `std::string urdf_path`：运动学模型来源，运行时加载 URDF。
- `std::string ee_frame_name`：末端 frame 名称，FK/Jacobian 都依赖它。
- `std::vector<std::string>`：明确右臂/左臂关节集合，后续 IK 可只优化右臂并锁定左臂。

为什么不用硬编码数组写死在类里：
- 相同逻辑可复用于不同机器人命名。
- 类型层面直接表达“这是一个有序关节列表”。

### 2.2 `NamedPoseMap` 类型为什么是嵌套 `unordered_map`

```cpp
using NamedPoseMap = std::unordered_map<std::string,
                  std::unordered_map<std::string, double>>;
```

语义是：
- 第一层 key：姿态名（如 `idle`）
- 第二层 key：关节名
- value：关节目标角

这样定义使得 JSON 命名姿态可以无损映射到内存结构，查找 `pose_name/joint_name` 都是平均 O(1)。

### 2.3 `loadNamedPoses` 里为何要做 joint limit clamp

`q_pose[ji.pin_q_adr] = std::clamp(joint_target, lower, upper);`

原因很直接：配置文件是外部输入，不能假设一定在关节上下限内。这里提前夹紧，比把非法值带到规划器/控制器里更安全。

### 2.4 轨迹相关函数的入参回参意义

- `bool ensureTrajectoryForState(const Command&, int state_id, double now)`
  - 入参：状态机命令 + 当前状态 id + 当前时间。
  - 回参 `bool`：本次是否有有效轨迹（`trajectory.valid`）。
  - 这样上层可以不用关心 planner 细节，只关心“这帧有没有可执行轨迹”。

- `Eigen::VectorXd sampleJointTargets(double now) const`
  - 回参是动态维向量，因为机械臂自由度由 URDF 决定，不能用固定维模板。
  - 若当前无轨迹：返回 hold 位或 currentQ，保证输出连续。

- `Eigen::VectorXd smoothJointTargets(const Eigen::VectorXd& q_target,
  const ControlLimits& limits, double dt) const`
  - `q_target` 是规划目标。
  - `limits` 区分手臂和手指速度上限。
  - `dt` 显式传入而不是内部读取，保证函数对时基透明，可测性更好。

### 2.5 为什么 `smoothJointTargets` 是“按关节限幅增量”

核心逻辑：
- 每个关节计算 `delta = q_target[i] - smoothed_q_cmd_[i]`
- 本帧最多走 `step_limit = v_max * dt`
- `delta` 经过 `clamp` 后累加

这比直接低通滤波更可控：速度上限是确定的物理量，不受目标阶跃幅值影响。

---

## 3. 状态机：`GraspStateMachine`

关键文件：
- `include/state_machine/grasp_state_machine.hpp`
- `src/state_machine/grasp_state_machine.cpp`

### 3.1 `Command` 结构体为何同时支持两种模式

```cpp
enum class CommandMode { JointPose, CartesianPose };
```

- `JointPose`：用于回 `idle` 等命名姿态，直接给关节目标。
- `CartesianPose`：用于 pregrasp/descend/lift，给末端位姿目标。

统一封装在一个 `Command` 里，上层 runtime 不需要切换两套接口。

### 3.2 `update(..., const ObjectPose* observed_pose, bool active_joint_pose_reached)`

入参解释：
- `double now`：状态切换依赖时间窗（例如 close 持续时长）。
- `Eigen::Vector3d current_pos`：当前末端位置，用于容差判断。
- `const ObjectPose* observed_pose`：可空指针表示“本帧没观测到目标”。
- `bool active_joint_pose_reached`：由任务层反馈是否到达命名姿态。

为什么用“可空指针”而不是引用：
- 引用不能表达缺测。
- `std::optional<ObjectPose>` 也可行，但这里与现有 C++/LCM 接口对接，指针成本最低。

### 3.3 姿态计算关键：`makeTopDownGraspQuat`

函数先取物体局部 X 轴在世界系投影求 `yaw`，再乘一个绕 Y 轴 `pi` 的工具对齐旋转，得到“顶抓”四元数。

实现里对四元数做了 normalize，避免数值漂移导致 IK 误差累积。

### 3.4 状态推进判据

- `Home -> Pregrasp`：达到 idle 且超过 `home_duration_sec`
- `Pregrasp -> Descend`：末端到 pregrasp 目标距离小于 `pregrasp_tolerance`
- `Descend -> Close`：到下降目标小于 `descend_tolerance`
- `Close -> Lift`：时间超过 `close_duration_sec`
- `Lift -> Done`：到抬升目标小于 `lift_tolerance`

这些判据统一写成“时间条件 + 几何误差条件”，便于调参时直接关联到配置项。

---

## 4. 规划器：`InterpolationMotionPlanner`

关键文件：
- `include/task/interpolation_motion_planner.hpp`
- `src/task/interpolation_motion_planner.cpp`

### 4.1 `createPlan(const MotionRequest&) -> PlannedTrajectory`

回参是完整轨迹对象，不是单点命令。原因：
- 执行层 `TrajectoryExecutor` 需要“可按时间采样”的轨迹。
- 轨迹对象包含 `owner_state_id / duration / points / valid`，上层可做状态一致性检查。

### 4.2 为何先 `resolveGoalQ`

`MotionRequest` 支持两类目标：
- `JointGoal`：直接用 `joint_goal_q`
- `CartesianPoseGoal`：先调用 `solveRightArmIK` 转成关节目标

即：先把“异构目标”统一成 `goal_q`，后续插值逻辑只处理一类数据（关节空间向量）。

### 4.3 插值公式为什么用 smoothstep

```cpp
smooth_alpha = alpha * alpha * (3 - 2 * alpha)
```

这是一种常见三次平滑插值，边界一阶导为 0，起终点速度不会突变，比线性插值更适合直接下发到位置控制器。

---

## 5. 运动学与 IK：`RobotKinematics`

关键文件：
- `include/robot/robot_kinematics.hpp`
- `src/robot/robot_kinematics.cpp`

### 5.1 为什么状态接口用 `Eigen::VectorXd`

`setRobotState(const Eigen::VectorXd& q, const Eigen::VectorXd& dq)`：
- `nq/nv` 由模型决定，运行期可变。
- `VectorXd` 允许同一实现兼容不同自由度模型。

### 5.2 `solveIK` 的入参与回参设计

```cpp
Eigen::VectorXd solveIK(const Eigen::Vector3d& target_pos,
                        const Eigen::Quaterniond& target_quat,
                        const Eigen::VectorXd& seed_q) const;
```

- `target_pos` 用 `Vector3d`：位置维度固定 3。
- `target_quat` 用 `Quaterniond`：旋转表达无奇异点（对比欧拉角）。
- `seed_q`：给迭代法初值与零空间偏置参考。
- 回参 `VectorXd`：输出完整关节解，直接可下发。

### 5.3 IK 关键实现点

1. 每次迭代计算当前位姿误差（位置 + 角轴旋转误差）。
2. 从全雅可比提取右臂列，构造 `jacobian_right`。
3. 用阻尼伪逆（`lambda=5e-3`）提升奇异位姿附近数值稳定性。
4. 叠加零空间项 `seed_bias`，使解更靠近 seed。
5. 每轮更新后按模型上下限 `clamp` 关节角。

这套写法的重点是“数值稳定 + 可控偏好 + 关节限位安全”。

---

## 6. 一条完整数据链（代码视角）

每个仿真周期关键路径：

1. `ClosedLoopRuntime::step` 发布真值并拉取观测。
2. `syncRobotStateToTask` 把 MuJoCo 状态映射到 Pinocchio 向量。
3. `GraspStateMachine::update` 生成统一 `Command`。
4. `ManipulateTask::ensureTrajectoryForState` 确保有当前状态轨迹。
5. `sampleJointTargets` + `smoothJointTargets` 得到限速后的 `q_cmd`。
6. 写入 `ctrl/qfrc_applied` 执行控制。

核心代码价值在于：
- 类型定义直接表达语义（可空观测、动态维关节、统一命令结构）。
- 关键边界都在代码层处理（限速、限位、缺测、状态切换）。
- 模块之间通过小而稳定的函数接口连接，主循环可读性高。
